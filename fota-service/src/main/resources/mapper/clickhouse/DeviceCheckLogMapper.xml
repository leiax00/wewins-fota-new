<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wewins.fota.clickhouse.mapper.DeviceCheckLogMapper">

    <!-- 插入单条检查日志 -->
    <insert id="insert" parameterType="com.wewins.fota.clickhouse.entity.DeviceCheckLog">
        INSERT INTO device_check_logs (
            event_time, device_id, imei, product_id, policy_id,
            current_version, target_version, target_version_id, decision,
            check_mode, language, device_tags, ext_tags, is_dev,
            has_update, gray_bucket, is_gray_hit,
            response_check_interval, download_delay,
            request_id,
            client_ip, user_agent, region,
            error_code, error_message
        ) VALUES (
            #{eventTime}, #{deviceId}, #{imei}, #{productId}, #{policyId},
            #{currentVersion}, #{targetVersion}, #{targetVersionId}, #{decision},
            #{checkMode}, #{language}, #{deviceTags}, #{extTags}, #{isDev},
            #{hasUpdate}, #{grayBucket}, #{isGrayHit},
            #{responseCheckInterval}, #{downloadDelay},
            #{requestId},
            toIPv4OrNull(#{clientIp}), #{userAgent}, #{region},
            #{errorCode}, #{errorMessage}
        )
    </insert>

    <!-- 批量插入检查日志 -->
    <insert id="insertBatch">
        INSERT INTO device_check_logs (
            event_time, device_id, imei, product_id, policy_id,
            current_version, target_version, target_version_id, decision,
            check_mode, language, device_tags, ext_tags, is_dev,
            has_update, gray_bucket, is_gray_hit,
            response_check_interval, download_delay,
            request_id,
            client_ip, user_agent, region,
            error_code, error_message
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.eventTime}, #{item.deviceId}, #{item.imei}, #{item.productId}, #{item.policyId},
             #{item.currentVersion}, #{item.targetVersion}, #{item.targetVersionId}, #{item.decision},
             #{item.checkMode}, #{item.language}, #{item.deviceTags}, #{item.extTags}, #{item.isDev},
             #{item.hasUpdate}, #{item.grayBucket}, #{item.isGrayHit},
             #{item.responseCheckInterval}, #{item.downloadDelay},
             #{item.requestId},
             toIPv4OrNull(#{item.clientIp}), #{item.userAgent}, #{item.region},
             #{item.errorCode}, #{item.errorMessage})
        </foreach>
    </insert>

</mapper>
