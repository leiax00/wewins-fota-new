<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wewins.fota.clickhouse.mapper.DeviceUpgradeEventMapper">

    <!-- 插入单条升级事件 -->
    <insert id="insert" parameterType="com.wewins.fota.clickhouse.entity.DeviceUpgradeEvent">
        INSERT INTO device_upgrade_events (
            event_id, imei, request_id, policy_id,
            event_type, download_url, details,
            device_id, product_id, firmware_version,
            event_time,
            client_ip, region
        ) VALUES (
            #{eventId}, #{imei}, #{requestId}, #{policyId},
            #{eventType.dbValue}, #{downloadUrl}, #{details},
            #{deviceId}, #{productId}, #{firmwareVersion},
            #{eventTime},
            toIPv4OrNull(#{clientIp}), #{region}
        )
    </insert>

    <!-- 批量插入升级事件 -->
    <insert id="insertBatch">
        INSERT INTO device_upgrade_events (
            event_id, imei, request_id, policy_id,
            event_type, download_url, details,
            device_id, product_id, firmware_version,
            event_time,
            client_ip, region
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.eventId}, #{item.imei}, #{item.requestId}, #{item.policyId},
             #{item.eventType.dbValue}, #{item.downloadUrl}, #{item.details},
             #{item.deviceId}, #{item.productId}, #{item.firmwareVersion},
             #{item.eventTime},
             toIPv4OrNull(#{item.clientIp}), #{item.region})
        </foreach>
    </insert>

</mapper>
