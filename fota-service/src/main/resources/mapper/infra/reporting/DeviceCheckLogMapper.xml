<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wewins.fota.infra.persistence.clickhouse.reporting.mapper.DeviceCheckLogMapper">

    <insert id="insert" parameterType="com.wewins.fota.domain.reporting.model.aggregate.DeviceCheckLog">
        INSERT INTO device_check_logs (
            event_time, device_id, imei, product_id, policy_id, download_url,
            version, internal_version, target_version, target_version_id, check_rst,
            check_mode, language, ext_tags, is_dev,
            gray_bucket, is_gray_hit,
            response_check_interval, download_delay,
            request_id,
            client_ip, user_agent, region,
            error_code, error_message
        ) VALUES (
            #{eventTime}, #{deviceId}, #{imei}, #{productId}, #{policyId}, #{downloadUrl},
            #{version}, #{internalVersion}, #{targetVersion}, #{targetVersionId}, #{checkRst.code},
            #{checkMode.code}, #{language}, #{extTags}, #{isDev},
            #{grayBucket}, #{isGrayHit},
            #{responseCheckInterval}, #{downloadDelay},
            #{requestId},
            toIPv4OrNull(#{clientIp}), #{userAgent}, #{region},
            #{errorCode}, #{errorMessage}
        )
    </insert>

    <insert id="insertBatch">
        INSERT INTO device_check_logs (
            event_time, device_id, imei, product_id, policy_id, download_url,
            version, internal_version, target_version, target_version_id, check_rst,
            check_mode, language, ext_tags, is_dev,
            gray_bucket, is_gray_hit,
            response_check_interval, download_delay,
            request_id,
            client_ip, user_agent, region,
            error_code, error_message
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.eventTime}, #{item.deviceId}, #{item.imei}, #{item.productId}, #{item.policyId}, #{item.downloadUrl},
             #{item.version}, #{item.internalVersion}, #{item.targetVersion}, #{item.targetVersionId}, #{item.checkRst.code},
             #{item.checkMode.code}, #{item.language}, #{item.extTags}, #{item.isDev},
             #{item.grayBucket}, #{item.isGrayHit},
             #{item.responseCheckInterval}, #{item.downloadDelay},
             #{item.requestId},
             toIPv4OrNull(#{item.clientIp}), #{item.userAgent}, #{item.region},
             #{item.errorCode}, #{item.errorMessage})
        </foreach>
    </insert>

</mapper>
